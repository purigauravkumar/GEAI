# GEAI local data directory. Keep this outside the Git repository.
GEAI_HOME=~/GEAI

# Recommended for the HTTP API. The current API code should be extended to
# enforce this value before exposing GEAI beyond localhost.
GEAI_API_KEY=replace-with-a-long-random-secret
